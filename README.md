# GPT Mini Mini

## Training

```bash
cd runs
python helper/train.py +configs=<config>
```

## Installation

```bash
docker-compose up --build -d
docker-compose down
docker exec -it gptmm-dev /bin/bash
```
